class IngresoOEgreso {
  constructor(codigo, nombre, descripcion, tipo, categoria) {
    (this.codigo = codigo),
      (this.nombre = nombre),
      (this.descripcion = descripcion),
      (this.tipo = tipo),
      (this.categoria = categoria);
  }
}